!function(a) {
    a.fn.dropMenu = function(b) {
        var c = a(this), d = a.extend({
            title: "Menu",
            format: "dropdown",
            sticky: !1
        }, b);
        return this.each(function() {
            return c.prepend('<div id="menu-hamburger">' + d.title + "</div>"), a(this).find("#menu-hamburger").on("click", function() {
                a(this).toggleClass("menu-opened");
                var b = a(".navigation");
                b.hasClass("open") ? (console.log("Close"), b.slideToggle().removeClass("open")) : (b.slideToggle().addClass("open"), 
                "dropdown" === d.format && b.find("ul").show());
            }), c.find("li ul").parent().addClass("dropdown"), multiTg = function() {
                c.find(".dropdown").prepend('<span class="submenu-button"></span>'), c.find(".submenu-button").on("click", function() {
                    a(this).toggleClass("submenu-opened"), a(this).siblings("ul").hasClass("open") ? a(this).siblings("ul").removeClass("open").slideToggle() : a(this).siblings("ul").addClass("open").slideToggle();
                });
            }, "multitoggle" === d.format ? multiTg() : c.addClass("dropdown"), d.sticky === !0 && c.css("position", "fixed"), 
            resizeFix = function() {
                a(window).width() > 768 && c.find("ul").show(), a(window).width() <= 768 && c.find("ul").hide().removeClass("open");
            }, resizeFix(), a(window).on("resize", resizeFix);
        });
    };
}(jQuery), function(a) {
    a(document).ready(function() {
        a("#site-navigation").dropMenu({
            title: "Menu",
            format: "multitoggle"
        });
    });
}(jQuery);